package com.example.medigo.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/search")
public class SearchController {

    // Si es paciente, mostrar los resultados del medico en paginas
    // Buscar un Doctor en espefico por el nombre en paginas
    // Buscar exactamente a un doctor por su username(email)
}
